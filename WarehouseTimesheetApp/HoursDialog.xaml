<Window x:Class="WarehouseTimesheetApp.HoursDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Почасовая смена" Height="280" Width="360" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="WorkedCheckBox" Content="Сотрудник работал" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" Text="Отработанные часы:" Margin="0,0,0,6"/>
        <TextBox x:Name="HoursTextBox" Grid.Row="2" Text="8"/>

        <TextBlock Grid.Row="3" Text="Доплата (₽):" Margin="0,12,0,6"/>
        <TextBox x:Name="ExtraTextBox" Grid.Row="4" Text="0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Width="100" Click="OnCancel"/>
            <Button Content="Сохранить" Width="100" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
