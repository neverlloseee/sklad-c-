<Window x:Class="WarehouseTimesheetApp.CustomDayDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подработка / переработка" Height="220" Width="350" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="WorkedCheckBox" Content="Сотрудник работал в этот день" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" Text="Доплата (₽):" Margin="0,0,0,6"/>
        <TextBox x:Name="ExtraTextBox" Grid.Row="2" Text="0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Width="100" Click="OnCancel"/>
            <Button Content="Сохранить" Width="100" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
